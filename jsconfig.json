{
  "compilerOptions": {
    "target": "ES2015", // หรือสูงกว่า เช่น "ES2017", "ES2020"
    "lib": ["ES2015", "DOM"], // เพิ่ม ES2015 และ DOM เพื่อรองรับ Promise, async/await, และ API ของเบราว์เซอร์
    "module": "ESNext", // ใช้ ESNext ถ้าต้องการ module system ที่ทันสมัย
    "moduleResolution": "Node", // ใช้ Node resolution สำหรับการหาม็อดูล
    "strict": true, // คำแนะนำให้เปิด strict mode เพื่อการตรวจสอบที่เข้มงวด
    "jsx": "preserve", // ใช้สำหรับ Vue ที่มี JSX
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "exclude": ["node_modules", "dist"]
}
